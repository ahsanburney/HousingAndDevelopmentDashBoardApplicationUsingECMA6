class chartSettings extends chartServices{
	constructor(chartType) {
		super(chartType);
	}
	setValues(tags, descriptions){
	  this.tags = tags;
	  this.descriptions = descriptions;
	}
	colorCharts(chartSettings){}
}










